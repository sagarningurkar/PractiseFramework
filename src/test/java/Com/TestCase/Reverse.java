package Com.TestCase;

import java.util.Map;

import org.apache.commons.collections4.map.HashedMap;
import org.testng.annotations.Test;

public class Reverse {
	
	
		
		//String s = "Sagar Ningurkar";
		/*String rev = "";
		String temp = null;
		
		
		for (int i= s.length()-1; i>=0; i--)
		{
			rev = rev + s.charAt(i);
			s.length();
		}
		System.out.println("The reverse order is:" + rev);*/
		
		/*
		 * StringBuffer sb = new StringBuffer("Sagar Ningurkar"); sb.trimToSize();
		 * //sb.replace( str) sb.reverse(); System.out.println(sb);
		 */
		@Test
		public void click()
		{
			Map <Integer, Character> map = new HashedMap<Integer, Character>();
			
			Second sc = new Second();
		
			
			
			
		}
		
		
		
	}


